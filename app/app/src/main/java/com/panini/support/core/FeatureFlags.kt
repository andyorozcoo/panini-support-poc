package com.panini.support.core

/**
 * Local feature switches for the PoC scope.
 * They are intentionally simple and can later be replaced by remote config.
 */
object FeatureFlags {
    const val enableTicketCreation = true
    const val enablePriorityUpdates = true
}